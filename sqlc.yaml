version: "2"
sql:
  - engine: "sqlite"
    queries: "db/queries.sql"
    schema: "sql/schema.sql"
    gen:
      go:
        package: "db"
        out: "db"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
